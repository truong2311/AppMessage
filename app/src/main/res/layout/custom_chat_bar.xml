<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_color"
    >
    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/ivProfile_custom_chat_bar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:src="@drawable/avatar"
        android:cropToPadding="true"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="10dp"/>
    <TextView
        android:id="@+id/tvUserName_custom_chat_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/ivProfile_custom_chat_bar"
        android:layout_alignParentTop="true"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="18sp"
        android:text="@string/username"
        />
    <TextView
        android:id="@+id/tvStatus_custom_chat_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tvUserName_custom_chat_bar"
        android:layout_toEndOf="@+id/ivProfile_custom_chat_bar"
        android:layout_marginTop="4dp"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="14sp"
        android:text="@string/status"/>
</RelativeLayout>